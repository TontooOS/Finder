//! Finder views for the Finder app.
//!
//! One module per window area. The Tahoe-style root layout lives in
//! `finder.rs`; it is split into follow-up modules once areas grow real
//! behavior (filesystem listing, toolbar actions, status details).

pub mod finder;
